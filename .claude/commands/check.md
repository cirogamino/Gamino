---
description: Run the full pre-push check (lint, typecheck, test)
---

Run `npm run lint`, `npm run typecheck`, and `npm run test -- --run` in sequence. Report a concise pass/fail summary for each. If anything fails, show the relevant error output and stop — do not attempt fixes unless asked.
